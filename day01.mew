(op=> (lines "day01")
      (gmap string->number _)
      (gsplit-on not _)
      (gmap (inject +) _)
      (into #() _)
      (sort! _ >)
      ,(juxt (fun=> (get _ 0))
             (fun=> gen (gtake _ 3) ,(inject +)))
      prn)
